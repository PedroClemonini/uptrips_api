<IfModule mod_rewrite.c>
    RewriteEngine On

    # Handle Front Controller (all routes should go to index.php)
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>

